<div class="row healer">
    <% healer.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row tank">
    <% tank.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row melee">
    <% melee.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row ranged">
    <% ranged.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row caster">
    <% caster.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row limited">
    <% limited.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row role">
    <% role.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>

<div class="row misc">
    <% misc.forEach(icon => { %>
        <div class="icon">
            <%- include(icon); -%>
        </div>
    <% }); %>
</div>